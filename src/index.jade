
mixin table(projects)
  table.table
    thead
      tr
        th(width='10%') Project
        th
        th(width='10%') Maintainer
    tbody
      for project in projects
        +row(project)

mixin row(project)
  tr
    td: +project(project)
    td
      .markdown !{project.desc}
    td: +maintainer(project.maintainer)
  tr.module-desc
    td.blah(colspan=3, align='center')
      +badge-npm(project.npm)
      +badge-version(project.node)
      +badge-travis(project.repo)
      +badge-coverage(project.repo)
      +badge-david(project.repo)
      +badge-downloads(project.npm)
      +badge-gittip(project.maintainer.github)

mixin project(project)
  a(href='https://github.com/' + project.repo)= project.name

mixin maintainer(person)
  a(href='https://github.com/' + person.github)= person.name

mixin badge(url)
  img(src='http://img.shields.io/' + url.replace(/^\//, '') + '.svg?style=' + style)

mixin badge-npm(name)
  a(href='https://www.npmjs.org/package/' + name)
    +badge('/npm/v/' + name)

mixin badge-version(version)
  //- probably want different colors for different versions
  a(href='http://nodejs.org/download/')
    +badge('/badge/node.js->=_' + version + '-blue')

mixin badge-david(repo)
  a(href='https://david-dm.org/' + repo)
    +badge('/david/' + repo)

mixin badge-travis(repo)
  a(href='https://travis-ci.org/' + repo)
    +badge('/travis/' + repo)

mixin badge-coverage(repo)
  a(href='https://coveralls.io/r/' + repo)
    +badge('/coveralls/' + repo)

mixin badge-downloads(npm)
  a(href='https://npmjs.org/package/' + npm)
    +badge('/npm/dm/' + npm)

mixin badge-gittip(user)
  a(href='https://www.gittip.com/' + user)
    +badge('/gittip/' + user)

doctype
html.no-js
  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge')
    title jshttp
    meta(name='description', content='')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    link(href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css', rel='stylesheet')
    style
      include:uglify-css style.css
  body
    .navbar.navbar-inverse(role='navigation')
      .container
        .navbar-header
          a.navbar-brand(href='#') JSHTTP

    +table(projects)

    //- Google Analytics: change UA-XXXXX-X to be your site's ID.
    //-
      script
        (function(b,o,i,l,e,r){b.GoogleAnalyticsObject=l;b[l]||(b[l]=
        function(){(b[l].q=b[l].q||[]).push(arguments)});b[l].l=+new Date;
        e=o.createElement(i);r=o.getElementsByTagName(i)[0];
        e.src='//www.google-analytics.com/analytics.js';
        r.parentNode.insertBefore(e,r)}(window,document,'script','ga'));
        ga('create','UA-XXXXX-X');ga('send','pageview');
